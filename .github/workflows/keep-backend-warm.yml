name: Keep Backend Warm

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl https://YOUR-BACKEND-URL.onrender.com/api/dashboard-all
